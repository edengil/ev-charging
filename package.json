{
  "name": "ev-charge-manager",
  "version": "1.0.0",
  "description": "Eden Gil EV Charge Manager",
  "main": "app-source.js",
  "scripts": {
    "start": "node scripts/local-wevo-proxy.mjs",
    "build": "node scripts/build-dist.mjs",
    "inject": "node scripts/inject-app.mjs",
    "deploy": "npm run inject && npm run build && wrangler pages deploy dist --project-name=eden-ev-charge --branch=main",
    "cf:kv": "wrangler kv namespace create EV_BACKUP",
    "test:unit": "node --test tests/unit/*.mjs",
    "test:e2e": "npm run inject && playwright test --config e2e/playwright.config.mjs",
    "test:e2e:ui": "npm run inject && playwright test --config e2e/playwright.config.mjs --ui",
    "test": "npm run test:unit && npm run test:e2e",
    "e2e:server": "node scripts/e2e-server.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "wrangler": "^4.28.0"
  }
}
